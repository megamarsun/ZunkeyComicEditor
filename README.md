# ズンコミ (Zunkey Comic Editor)

漫画やイラストの「セリフ」を簡単に入れ替えたり、描き文字（オノマトペ）を貼り付けたりできる編集ツールです。
Python製で、直感的な操作と美しい縦書きレンダリング（Skiaエンジン採用）が特徴です。

## 📥 ダウンロード
実行ファイル（EXE版）は **note** にて配布しています。
インストール不要ですぐに使えます。

👉 **[ダウンロードはこちら (note)]**
https://note.com/megamarsun/n/n068cb84dd9a3

---

## 📖 使い方マニュアル

### 【1】 漫画画像を開く
1. 左上の **「1. ファイル操作」** > **[画像を開く (新規)]** で画像（JPG/PNG）を読み込みます。

### 【2】 文字を消す（消しゴム）
1. **「2. 消しゴム」** の **[スポイト]** ボタンを押し、画像内の背景色（白など）をクリックして色を取得します。
2. そのまま消したい文字の上をなぞって塗りつぶします。

### 【3】 文字を入れる
1. **「3. 文字入力」** にセリフを入力し、**[リストに登録]** を押します。
2. 下のリストからセリフをクリックし、画像の好きな場所をクリックして配置します。

### 【4】 文字の調整
* 配置した文字をクリックして選択すると、**「5. テキスト設定」** で編集できます。
* **フォント・色・サイズ** だけでなく、**縦書き・行間・文字間・回転・縁取り** も調整可能です。
* スライダー横の `+` `-` ボタンや数値入力で細かく調整できます。
* **フォントの追加:** フォント選択欄横の **[＋]** ボタンから手持ちのフォントファイル（.ttf/.otf）を追加できます。

### 【5】 描き文字（画像）を貼る
1. 右サイドバーの **[＋ 画像を追加]** で素材画像（背景透過PNG推奨）を読み込みます。
2. リストから選択し、画像上をクリックして配置します。
3. **「6. 画像設定」** で回転や拡大縮小が可能です。

### 【6】 保存・中断
* **[画像書出 (PNG)]**: 完成した画像を1枚の絵として保存します。
* **[プロジェクト保存]**: 作業状態（レイヤー構造や素材）を `.zmm` ファイルとして保存します。後で **[プロジェクトを開く]** から再開できます。

---

## 🛠️ 開発者向け情報
このリポジトリはソースコードの公開用です。
描画エンジンに `Skia` を使用しているため、以下のライブラリが必要です。

### 必要要件
* Python 3.10+
* skia-python
* numpy
* Pillow

### セットアップコマンド
```bash
pip install skia-python numpy pillow
python ZunkeyComicEditor.py